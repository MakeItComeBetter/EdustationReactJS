// import { createSelector } from 'reselect';
// import { getMessenger } from './CommonSelectors';

// export const currentMessagesAndLastVisible = createSelector(
//   getMessenger,
//   (currentMessages, lastVisible) => {
//     if (currentMessages) {
//       return {
//         currentMessages: currentMessages,
//         lastVisible: lastVisible
//       };
//     }

//     return null;
//   }
// );
